## Unifi Wifi CLI

CLI interacting with the Unifi API for toggling all access points. Useful when simply hiding Wifi SSIDs is insufficient.

## How to run

```bash
uv run main.py --no-disabled

uv run main.py disabled
```